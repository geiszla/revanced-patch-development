org.gradle.parallel = true
org.gradle.caching = true
kotlin.code.style = official
version = 1.1.0

# GitHub Packages authentication is read from ~/.gradle/gradle.properties
# (or the GITHUB_ACTOR/GITHUB_TOKEN env vars in CI). Set the following there:
#   gpr.user / gpr.key                              (plugin resolution)
#   githubPackagesUsername / githubPackagesPassword (patcher dependencies)
